namespace MeticulousResearch.App.Tests;

/// <summary>
/// Baseline smoke test proving the App.Tests project (net8.0-windows, references the WPF app)
/// builds and runs green. Replaced by real view-model tests as features land.
/// </summary>
public class SmokeTests
{
    [Fact]
    public void Baseline_holds() => Assert.True(true);
}
