<UserControl x:Class="MeticulousResearch.App.Views.ProjectsHomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:MeticulousResearch.App.ViewModels"
             d:DataContext="{d:DesignInstance vm:ProjectsHomeViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="24" x:Name="ProjectsHomeRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="SemiBold" />
            <TextBlock Text="{Binding Headline}" Margin="0,4,0,0" Foreground="#555" />
        </StackPanel>
        <Border Grid.Row="1" Margin="0,16,0,0" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6">
            <TextBlock Text="No projects yet — create your first research project."
                       HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#777" />
        </Border>
    </Grid>
</UserControl>
